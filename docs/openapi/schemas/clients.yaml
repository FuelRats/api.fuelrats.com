# Auto-generated from Client model + ClientView
# Regenerate with: bun run openapi:generate

Client:
  allOf:
    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
    - type: object
      properties:
        type:
          type: string
          enum: [clients]
        attributes:
          type: object
          properties:
            name:
              type: string
              description: "OAuth client display name (writable by: owner or admin)"
            redirectUri:
              type: string
              nullable: true
              description: "OAuth redirect URI (writable by: owner or admin)"
            namespaces:
              type: array
              items:
                type: string
              description: "Allowed OAuth scopes for this client (writable by: admin only)"
            firstParty:
              type: boolean
              description: "Whether this is a first-party Fuel Rats client (writable by: admin only)"
            createdAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            updatedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            deletedAt:
              type: string
              format: date-time
        relationships:
          type: object
          properties:
            user:
              type: object
              properties:
                data:
                  nullable: true
                  allOf:
                    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
                    - properties:
                        type:
                          enum: [user]
                links:
                  $ref: '../components/schemas.yaml#/Links'

ClientDocument:
  allOf:
    - $ref: '../components/schemas.yaml#/SingleResourceDocument'
    - properties:
        data:
          $ref: '#/Client'

ClientCollection:
  allOf:
    - $ref: '../components/schemas.yaml#/CollectionDocument'
    - properties:
        data:
          type: array
          items:
            $ref: '#/Client'
