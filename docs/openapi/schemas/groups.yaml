# Auto-generated from Group model + GroupView
# Regenerate with: bun run openapi:generate

Group:
  allOf:
    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
    - type: object
      properties:
        type:
          type: string
          enum: [groups]
        attributes:
          type: object
          properties:
            name:
              type: string
              description: Unique group identifier name
            displayName:
              type: string
              nullable: true
              description: Human-readable group display name
            vhost:
              type: string
              nullable: true
              description: "IRC virtual host suffix assigned to group members (writable by: admin only)"
            withoutPrefix:
              type: boolean
              description: "Whether the vhost is used without a rat name prefix (writable by: admin only)"
            priority:
              type: integer
              description: "Group priority for vhost and permission ordering (writable by: admin only)"
            permissions:
              type: array
              items:
                type: string
              description: "OAuth permission scopes granted to group members (writable by: admin only)"
            channels:
              type: object
              description: "IRC channel access flags for group members (writable by: admin only)"
            createdAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            updatedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            deletedAt:
              type: string
              format: date-time
              description: "Writable by: system only"

GroupDocument:
  allOf:
    - $ref: '../components/schemas.yaml#/SingleResourceDocument'
    - properties:
        data:
          $ref: '#/Group'

GroupCollection:
  allOf:
    - $ref: '../components/schemas.yaml#/CollectionDocument'
    - properties:
        data:
          type: array
          items:
            $ref: '#/Group'
