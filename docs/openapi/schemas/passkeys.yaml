# Auto-generated from Passkey model + PasskeyView
# Regenerate with: bun run openapi:generate

Passkey:
  allOf:
    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
    - type: object
      properties:
        type:
          type: string
          enum: [passkeys]
        attributes:
          type: object
          properties:
            credentialId:
              type: string
              description: WebAuthn credential identifier
            publicKey:
              type: string
              description: WebAuthn credential public key
            counter:
              type: integer
              description: WebAuthn signature counter for replay detection
            name:
              type: string
              description: User-assigned passkey label
            backedUp:
              type: boolean
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time
        relationships:
          type: object
          properties:
            user:
              type: object
              properties:
                data:
                  nullable: true
                  allOf:
                    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
                    - properties:
                        type:
                          enum: [user]
                links:
                  $ref: '../components/schemas.yaml#/Links'

PasskeyDocument:
  allOf:
    - $ref: '../components/schemas.yaml#/SingleResourceDocument'
    - properties:
        data:
          $ref: '#/Passkey'

PasskeyCollection:
  allOf:
    - $ref: '../components/schemas.yaml#/CollectionDocument'
    - properties:
        data:
          type: array
          items:
            $ref: '#/Passkey'
