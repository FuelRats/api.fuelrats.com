# Auto-generated from Rat model + RatView
# Regenerate with: bun run openapi:generate

Rat:
  allOf:
    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
    - type: object
      properties:
        type:
          type: string
          enum: [rats]
        attributes:
          type: object
          properties:
            name:
              type: string
              description: "Commander name (writable by: owner or admin)"
            data:
              type: object
              description: "Arbitrary rat metadata (writable by: owner or admin)"
            platform:
              type: string
              enum: [pc, xb, ps]
              description: "Gaming platform (writable by: owner or admin)"
            expansion:
              type: string
              enum: [horizons3, horizons4, odyssey]
              description: "Game expansion version (writable by: owner or admin)"
            frontierId:
              type: integer
              nullable: true
              description: "Frontier Developments commander identifier (writable by: system only)"
            createdAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            updatedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            deletedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
        relationships:
          type: object
          properties:
            user:
              type: object
              properties:
                data:
                  nullable: true
                  allOf:
                    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
                    - properties:
                        type:
                          enum: [user]
                links:
                  $ref: '../components/schemas.yaml#/Links'

RatDocument:
  allOf:
    - $ref: '../components/schemas.yaml#/SingleResourceDocument'
    - properties:
        data:
          $ref: '#/Rat'

RatCollection:
  allOf:
    - $ref: '../components/schemas.yaml#/CollectionDocument'
    - properties:
        data:
          type: array
          items:
            $ref: '#/Rat'
