# Auto-generated from Decal model + DecalView
# Regenerate with: bun run openapi:generate

Decal:
  allOf:
    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
    - type: object
      properties:
        type:
          type: string
          enum: [decals]
        attributes:
          type: object
          properties:
            code:
              type: string
              description: "Frontier store redemption code (writable by: admin only)"
            type:
              type: string
              enum: [Rescues, Promotional, Special]
              description: "Decal award category (writable by: admin only)"
            notes:
              type: string
              description: "Administrative notes about this decal (writable by: admin only)"
            claimedAt:
              type: string
              format: date-time
              nullable: true
              description: Date the decal was claimed by a user
            createdAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            updatedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
            deletedAt:
              type: string
              format: date-time
              description: "Writable by: system only"
        relationships:
          type: object
          properties:
            user:
              type: object
              properties:
                data:
                  nullable: true
                  allOf:
                    - $ref: '../components/schemas.yaml#/ResourceIdentifier'
                    - properties:
                        type:
                          enum: [user]
                links:
                  $ref: '../components/schemas.yaml#/Links'

DecalDocument:
  allOf:
    - $ref: '../components/schemas.yaml#/SingleResourceDocument'
    - properties:
        data:
          $ref: '#/Decal'

DecalCollection:
  allOf:
    - $ref: '../components/schemas.yaml#/CollectionDocument'
    - properties:
        data:
          type: array
          items:
            $ref: '#/Decal'
