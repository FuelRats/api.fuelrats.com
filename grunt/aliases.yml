default:
  description: Default Grunt task
  tasks:
  - 'server'
  - 'buildTest'
  - 'watch'

server:
  tasks:
  - 'shell:mongo'
  - 'shell:api'

buildTest:
  tasks:
  - 'browserify'

test:
  tasks:
  - 'buildTest'
  - 'shell:mongo'
  - 'shell:api'
  - 'mocha:test'

coverage:
  tasks:
  - 'buildTest'
  - 'shell:mongo'
  - 'shell:api'
  - 'mocha:coverage'
