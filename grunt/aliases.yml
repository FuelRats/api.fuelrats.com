default:
  description: Default Grunt task
  tasks:
  - 'server'
  - 'watch'

server:
  tasks:
  - 'shell:mongo'
  - 'shell:api'

test:
  tasks:
  - 'shell:mongo'
  - 'shell:api'
  - 'browserify'
  - 'mocha:test'

coverage:
  tasks:
  - 'shell:mongo'
  - 'shell:api'
  - 'browserify'
  - 'mocha:coverage'
