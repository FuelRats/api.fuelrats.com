api:
  description: Start the api
  tasks:
  - 'cssmin:production'
  - 'buildJS'
  - 'uglify:production'
  - 'express'
  - 'watch'

buildJS:
  tasks:
  - 'babel'

default:
  description: Default Grunt task
  tasks:
  - 'api'

test:
  description: Run the tests
  tasks:
  - 'express'
