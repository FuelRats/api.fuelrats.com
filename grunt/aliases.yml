api:
  description: Start the api
  tasks:
  - 'docs'
  - 'express'
  - 'watch'

default:
  description: Default Grunt task
  tasks:
  - 'api'

docs:
  description: Build the docs
  tasks:
  - 'run:docs'
