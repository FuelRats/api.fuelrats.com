api:
  description: Start the api
  tasks:
  - 'shell:mongo'
  - 'shell:api'

coverage:
  description: Not working yet. :-/
  tasks:
  - 'api'
  - 'mocha:coverage'

default:
  description: Default Grunt task
  tasks:
  - 'server'
  - 'browserify'
  - 'watch'

server:
  description: Just an alias for the api task
  tasks:
  - 'api'

test:
  description: Run the tests
  tasks:
  - 'browserify'
  - 'api'
  - 'mocha:test'
