'use strict'

let Rescue = require('../db').Rescue
let Errors = require('../errors')

class Controller {
  static read (query) {
    return new Promise(function (resolve, reject) {

    })
  }
}

function isEntitledToDecal(user) {
  if ()
}